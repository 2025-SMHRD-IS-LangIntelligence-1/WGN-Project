<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.smhrd.web.mapper.FeedMapper">

	<insert id="saveFeed" parameterType="t_feed" useGeneratedKeys="true" keyProperty="feed_idx">
    	INSERT INTO t_feed (mb_id, res_idx, feed_content, feed_likes, created_at)
    	VALUES (#{mb_id}, #{res_idx}, #{feed_content}, 0, NOW())
	</insert>

	<insert id="saveFeedImg">
	    INSERT INTO t_feed_img (feed_img_idx, feed_idx, feed_img_url)
	    VALUES
	    <foreach collection="imgUrls" item="url" separator=",">
	        (NULL, #{feed_idx}, #{url})
	    </foreach>
	</insert>
	
	<select id="selectFeedByMemId">
		SELECT f.feed_idx, f.mb_id, f.res_idx, f.feed_content, f.feed_likes, f.created_at, m.mb_nick
		FROM t_feed f
		left JOIN t_member m on f.mb_id=m.mb_id
		where f.mb_id=#{mb_id};
	</select>
	
	<select id="selectFeedByIdx">
		SELECT f.feed_idx, f.mb_id, f.res_idx, f.feed_content, f.feed_likes, f.created_at, m.mb_nick
		FROM t_feed f
		left JOIN t_member m on f.mb_id=m.mb_id
		where feed_idx=#{feed_idx};
	</select>

</mapper>

